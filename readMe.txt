To open the application, open the folder called "nodejs-proj" in VSCode, 
pull up a terminal and type "npm install" to install dependencies. 
Then, type node "index.js". 
When you see on the terminal that it is listening on port 5155, go to localhost:5155 and the application should open.